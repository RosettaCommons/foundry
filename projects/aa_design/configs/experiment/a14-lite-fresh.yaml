# @package _global_

defaults:
  - override /model: unet-small

model:
  net:
    diffusion_module:
        diffusion_transformer:
          n_registers: 10

name: train-unet-detrunked-pretrainedweights
tags:
  - experiment
  - atom14
  - detrunked
  - unet
  - freshweights
  - unet-small
  - ${datasets.global_transform_args.central_atom}

trainer:
  prevalidate: True

# datasets:
#   cif_parser_args:
#     save_to_cache: True

ckpt_path: null
