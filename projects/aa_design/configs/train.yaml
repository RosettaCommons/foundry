# @package _global_
hydra:
  searchpath:
    - pkg://configs

defaults:
  - model: unet
  - trainer: aa_design
  - datasets: design_base
  - callbacks: design_callbacks
  - dataloader: fast
  - paths: default
  - hydra: default
  - logger: default
  - _self_
  # Required overrides:
  - experiment: ???
  - debug: null

# Placeholders
name: ???
tags: ???
# Definitions:
task_name: train
project: aa_design
seed: 42
ckpt_path: null
