# @package _global_

name: af3-protein-ligand-only

defaults:
  - override /datasets: af3_pdb_only
  - override /model: af3
  - override /trainer: af3

tags:
  - af3

project: af3

ckpt_path: /net/tukwila/ncorley/modelhub/inference/rf2aa-af3-repro7_ep680.pt

datasets:
  val: 
    af3_validation:
      dataset:
        dataset:
            filters:
              - "interfaces_to_score.str.contains('protein-ligand')"
              # Exclude example where RDKit errors
              - example_id not in ["{['validation']}{7qbs}{1}{[]}"]

