# @package _global_
# Ablation experiment base for RFD3


defaults:
  - rfd3-ablation-control
  - _self_

name: rfd3-ablation-blt

model:
  net:
    diffusion_module:
      downcast:
        method: mean

      upcast:
        method: broadcast
